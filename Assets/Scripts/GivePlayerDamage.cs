using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GivePlayerDamage : MonoBehaviour
{
    //public int damageAmount = 10;
    //public float cooldownTime = 2.0f;
    //private float nextDamageTime = 0.0f;

    //void OnTriggerStay2D(Collider2D other)
    //{
    //    if (Time.time >= nextDamageTime)
    //    {
    //        nextDamageTime = Time.time + cooldownTime;
    //        PlayerMovement dolphinHealthScript = other.GetComponent<PlayerMovement>();

    //        if (dolphinHealthScript != null)
    //        {
    //            dolphinHealthScript.TakeDamage(damageAmount);
    //        }
    //    }

    //}
}
